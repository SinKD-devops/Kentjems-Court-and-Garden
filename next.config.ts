import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The floating dev badge sits over the fixed bottom bar, which is exactly
  // where this app puts its primary action.
  devIndicators: false,
};

export default nextConfig;
